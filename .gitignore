node_modules/
dist/
build/
.env
.env.local
*.log
.DS_Store
Thumbs.db
apps/api/prisma/*.db
apps/api/prisma/**/*.db
apps/api/prisma/**/*.db-journal
apps/api/prisma/sqlite/tmp/
apps/api/prisma/sqlite/.generated/
uploads/
coverage/
.vite/
*.tsbuildinfo

# Claude Code: share project agents/skills, but never personal/local settings
.claude/settings.local.json

# local-only compose override (test Postgres)
docker-compose.override.yml

# playwright e2e artifacts
apps/web/test-results/
apps/web/playwright-report/
apps/web/.playwright-mcp/

# local build artifacts (installers)
release-artifacts/

# Claude Code plugin ships a self-contained MCP bundle (committed so /plugin install
# works straight from GitHub without a build step). Force it past the node_modules rule.
!packages/claude-plugin/mcp/
!packages/claude-plugin/mcp/**
