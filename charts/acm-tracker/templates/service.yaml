apiVersion: v1
kind: Service
metadata:
  name: {{ include "acm-tracker.fullname" . }}
  labels:
    {{- include "acm-tracker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "acm-tracker.selectorLabels" . | nindent 4 }}
